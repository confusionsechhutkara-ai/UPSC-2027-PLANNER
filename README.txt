211 DAY PLANNER PRO MAX
Open index.html to use.
For true Add to Home Screen/PWA installation, host the folder on HTTPS (for example a static hosting service).
Your progress is stored in the browser. Use Backup JSON regularly if you change phones or browsers.
